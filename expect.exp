#!/usr/bin/expect
#!/bin/sh

set timeout 20
spawn calabash-ios setup build/iphone/
expect "Please answer yes (y) or no (n)"  { send "n\r" }
expect "Default target: meineTuiTablet"  {  send "\r" }
expect "Setup done"

spawn sleep 5
spawn ti calabash --platform=ios
expect "Please answer yes (y) or no (n)"  { send "n\r" }
expect "Setup done"
set timeout 60

expect "** BUILD SUCCEEDED **"
